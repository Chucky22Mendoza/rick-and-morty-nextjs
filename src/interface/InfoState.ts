export default interface IInfoState {
  /**
   * Represents a property 'info' of type IInfo.
   */
  info: IInfo;
  /**
   * Represents a function 'setInfo' that takes a parameter 'info' of type IInfo and returns void.
   * @param info The info to be added.
   * @returns void
   */
  setInfo: (info: IInfo) => void;
}
